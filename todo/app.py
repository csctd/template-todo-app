import todobricks


def rank_by_project(FIXME):
    '''
    writes a new version of the todolist with all of the tasks sorted by project
    and the project with the most tasks first
    '''


def sort_by_due_date(FIXME):
    '''
    writes a new version of the todolist that is sorted by due date
    '''


def rank_by_context(FIXME):
    '''
    writes a new version of the todolist with all of the tasks sorted by context
    and the context with the most tasks first
    '''
